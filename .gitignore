# Python & uv
__pycache__/
*.py[cod]
*$py.class
build/
dist/
wheels/
*.egg-info/
.venv/
.pytest_cache/
.tox/
.uv/

# Emacs
*~
\#*\#
.\#*

# nano
.nano_history

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
desktop.ini

# Linux / FreeBSD
*~
core

# project
config.toml
# elixir / erlang
/_build/
/deps/
/cover/
/doc/
/.fetch
erl_crash.dump
*.ez
merlind-*.tar
merlind-*.tar.gz
/.elixir_ls/

# reaper: results are pulled back into out/ and are never committed
/out/

# secrets: a real house's config and secrets never enter this repository.
# `config/` here is Elixir's compile-time configuration, which is committed;
# the runtime files live at /usr/local/etc/merlin and are named for the house.
merlin.secrets.exs
/merlin.exs
